### Pizza Order REST APIs

#### Commands to run locally

```shell script
composer install
php artisan migrate:refresh --seed
php artisan serve
```

#### Used packages

- Passport for authentication
- Faker to load dummy data
